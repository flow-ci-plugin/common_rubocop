name: rubocop_plugin
version: 0.0.1
language: 
description: A Ruby static code analyzer, based on the community Ruby style guide.
tags:
 - ruby
 - rubocop
 - format
 - analyzer
 - plugin

public: true

# clone_in_docker: false


outputs:
  - name: FLOW_RUBOCOP_LOG_PATH
  - name: FLOW_RUBOCOP_SUMMARY_OFFENSE_COUNT
  - name: FLOW_RUBOCOP_SUMMARY_TARGET_FILE_COUNT
  - name: FLOW_RUBOCOP_SUMMARY_INSPECTED_FILE_COUNT

script_path: ./run.sh
